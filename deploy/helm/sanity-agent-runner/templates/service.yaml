apiVersion: v1
kind: Service
metadata:
  name: {{ include "sar.fullname" . }}
  labels: {{- include "sar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "sar.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
